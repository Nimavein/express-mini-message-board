extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href="/new")= "add" 

  ul.no-bullets 
    each i in messages
      .single-message 
        li= i.user
        li= i.text 
        li= i.added 